<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ebookfrenzy.sampleimagebutton">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="St. Thomas Malankara Catholic Parish"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".FloridaLocation"
            android:label="@string/title_activity_florida_location"></activity>
        <activity
            android:name=".IllinoisLocation"
            android:label="@string/title_activity_illinois_location" />
        <activity
            android:name=".MichiganLocation"
            android:label="@string/title_activity_michigan_location" />
        <activity
            android:name=".TexasLocation"
            android:label="@string/title_activity_texas_location" />
        <activity
            android:name=".maryLandLocations"
            android:label="@string/title_activity_mary_land_locations" />
        <activity
            android:name=".PhiladeplhiaLocations"
            android:label="@string/title_activity_philadeplhia_locations" />
        <activity
            android:name=".NewYorkLocations"
            android:label="@string/title_activity_new_york_locations" />
        <activity
            android:name=".NewJerseyLocations"
            android:label="@string/title_activity_new_jersey_locations" />
        <activity
            android:name=".CanadaPage"
            android:label="@string/title_activity_canada_page" />
        <activity
            android:name=".massStateActivity"
            android:label="Massachusetts" />
        <activity
            android:name=".CaliforniaLocations"
            android:label="@string/title_activity_california_locations" />
        <activity
            android:name=".MapsActivity"
            android:label="Locations" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>